# src/travel_companion_assignment

Installable package stub declared in `pyproject.toml`.

## Files

| File | Purpose |
| --- | --- |
| `__init__.py` | `main()` used by the `travel-companion-assignment` console script |

The interactive application is launched with `uv run python run.py`, not this stub. Keep this package if you rely on `uv` / pip install entry points; otherwise treat it as a placeholder.
